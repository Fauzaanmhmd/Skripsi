import Layout from "../../components/Layout"

export default function About({ recipes }) {
    return (
        <Layout>
            <div>
                <p>tester</p>
            </div>
        </Layout>

    )
}