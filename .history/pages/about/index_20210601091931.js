import Layout from "../../components/Layout"

export default function About({ recipes }) {
    return (
        <Layout>
            <div className="admin">
                tes
            </div>
        </Layout>

    )
}