import Layout from "../../components/Layout"

export default function Privacy({ privacy }) {
    const Privacy = props => {
        return (
            <Layout>
                <div>tester</div>
            </Layout>
        )
    }
}

export default Privacy