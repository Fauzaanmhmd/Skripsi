import Layout from "../../components/Layout"

export default function Privacy({ privacy }) {
    const Privacy {
        <div> tester</div>
}

return (
    <Layout>
        <Link href={`/catalogue/${Product.imageUrl}`}></Link>

    </Layout>
)
}

export defaul Privacy